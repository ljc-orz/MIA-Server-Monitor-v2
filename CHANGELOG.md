# 更新记录

当前版本：26.09.23

## 26.09.23（2026-09-23）

- 为 `gpustat` 和一次性 SSH 命令增加硬超时，避免单台故障服务器永久阻塞采集线程。
- `gpustat` 无响应时自动切换到 PCI/sysfs 降级探测，在控制台持续展示 NVIDIA 设备的 PCI、驱动和内核错误状态。
- 降级服务器使用黄灯闪烁；SSH 断线仍使用红灯，`gpustat` 恢复后自动切回正常终端和绿灯。
- 降级熔断状态可在监控进程重启后从 SQLite 恢复；恢复探测按服务器启动标识、PCI 状态变化和限频周期触发，避免故障期间反复调用失效的 `gpustat`。
- 优化降级模式展示：黄灯完整退回灰色后再闪烁，刷新时保留终端横纵滚动位置，并用红绿颜色突出设备状态。
- 降级终端和页面状态栏新增最后一次正常采集时间。
- 降低降级模式误报：非超时 JSON 错误需连续 3 次才触发降级，终端 watch 异常在 JSON 正常时仅独立重启。
- 恢复流程改为连续两次 JSON 成功且收到新 watch 帧后才切回正常，并为健康 PCI 状态提供快速重试及失败退避。
- 收紧内核错误判定：普通 Xid 仅展示，只有掉卡、驱动初始化失败及严重 Xid 才标红；新增 30 天采集事件记录用于追踪状态切换。
- 硬故障、降级及恢复判定均不查询远端进程状态，也不使用远端 D 状态进程作为信号。

## 26.08.21（2026-08-21）

- 新增持久化的服务器在线会话时间：在线显示本次首次在线时间，断线显示最后一次在线时间。
- 网页时间自动按浏览器本地时区格式化，并增加低调的时间含义说明。

## 26.08.14（2026-08-14）

- 新增脱敏 README，补充部署、配置、systemd 运维、安全边界和 Agent 交接说明。
- README 推荐使用支持 CUDA 设备顺序映射的定制版 `gpustat`。
- `servers.json` 现在是前后端共享的服务器清单；服务会热更新采集线程，网页会自动同步新增、删除和排序变化。

## 26.08.12（2026-08-12）

- 为每台服务器增加 `env` 配置，可在执行 `gpustat` 时注入服务器专属环境变量。
- 修改247的`gpustat`程序，使其为CUDA Runtime给出的device ordinal（CUDA_DEVICE_ORDER=FASTEST_FIRST）。
- 修复终端能力查询（DCS/XTGETTCAP）控制序列残留为 `+q...` 乱码的问题。
- 服务器掉线时保留最后一次成功采集时间，并以红色状态灯标识断线。
- 增加网页内更新记录入口。
